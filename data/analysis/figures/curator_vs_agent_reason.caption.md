**Supplementary Figure 4. Triage-agent reason agreement with the curator is high overall and concentrated in within-bucket reassignments, with the Sonnet 4.6 + NCBI production configuration scoring 87.1 % exact reason agreement on the 147-gene SurfaceBench.** Three-panel composite that walks the agent's `predicted_reason` against the curator's `ground_truth_reason` (both drawn from the same closed 19-value `TriageReason` enum) at three resolutions. **(a) Bucket-strict accuracy across all 10 model-variant cells** — for every (Haiku 4.5, Sonnet 4.6, Opus 4.8) × (naive, + IDs, + IDs + web, + IDs + PubMed) cell available on mainbench (Opus runs only naive + IDs), bar height is the mean-of-replicates fraction whose predicted-reason bucket matches the curator reason's bucket (Yes / Contextual / No). The grouping is strict — yes ↔ contextual mismatches are *not* soft-credited — so this is a tougher accuracy axis than Supp Fig 1. Overall bar (with dotted separator) plus per-bucket bars; overlaid dots are per-replicate accuracies and error bars are SEM. **(b) Per-reason accuracy across 4 frontier configs** — best-Haiku (web + IDs), Sonnet 4.6 (+ IDs and + IDs + PubMed), best-Opus (+ IDs). Each bar group is one reason from the 16-of-19 codes that appear in the bench, ordered left-to-right Yes → Contextual → No with x-tick labels colored by bucket (green / orange / grey) for at-a-glance bucket-stratification. **(c) Confusion matrix for the production configuration** — Sonnet 4.6 + NCBI predictions collapsed across three replicates by mainbench_canonical_v2's majority-then-first-rep rule, with rows / columns blocked by surface-verdict bucket (separated by light grey dividers); the diagonal carries 128 / 147 genes (87.1 % exact agreement) with red cell outlines, and off-diagonal cells with ≤ 3 genes additionally label the genes by name (the largest single bucket is cytoplasmic → inner_leaflet_anchored at 3 / 19: BTK, JAK1, AKT2 — a within-`no` reassignment that does not affect verdict accuracy).
