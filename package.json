{
  "name": "accessible-surfaceome-tooling",
  "private": true,
  "version": "0.0.0",
  "description": "Repo-root Node tooling. Currently just pins the wrangler CLI used by cloudflare/ scripts (D1 schema apply, D1 export, R2 upload). Not a runtime package — the viewer SPA has its own package.json under viewer/.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "wrangler": "wrangler"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
