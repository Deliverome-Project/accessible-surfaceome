data/raw/** filter=lfs diff=lfs merge=lfs -text
data/external/** filter=lfs diff=lfs merge=lfs -text
data/processed/** filter=lfs diff=lfs merge=lfs -text
data/analysis/** filter=lfs diff=lfs merge=lfs -text

# Figure-reproduction gist scripts + gist-context markdown ship as
# plain git, not LFS — they're tiny (1–13 KB each) and CI needs to
# read their contents directly (tests/test_figure_gists_styling.py
# greps each script for the brand-style sentinel + palette anchors;
# an LFS pointer there fails every assertion). Pattern recurses into
# per-analysis subdirs so a figure parked under
# data/analysis/<area>/make_*.py is also exempted — earlier
# `data/analysis/figures/*.py` rule didn't recurse, and the paywall
# figure ended up in LFS by accident.
data/analysis/**/make_*.py -filter text
data/analysis/**/01_*.md -filter text
data/analysis/figures/*.py -filter text
data/analysis/figures/*.md -filter text
# Gist registry + SWHID map are small JSON read by Python test code
# at collection time — LFS pointer reads as text that fails json.loads.
data/analysis/figures/gist_map.json -filter -diff -merge text
data/analysis/figures/swhid_map.json -filter -diff -merge text

# Figure outputs themselves ship as plain git too — each PDF is
# 20–60 KB and each PNG is 130–500 KB, well under the CLAUDE.md
# "LFS-track raster outputs ≥10 MB" threshold. Committing them
# directly means PR reviewers see the rendered artifacts inline
# (LFS pointers render as text blobs in GitHub's diff view), and
# raw.githubusercontent.com URLs embedded in PNG/PDF metadata
# resolve to the actual bytes instead of an LFS pointer. The
# `data/analysis/**` pattern catches per-analysis figure dirs
# (opentargets_vs_m1, candidate_universe_agreement, etc.) too —
# largest such file today is ~310 KB.
data/analysis/**/*.pdf  -filter -diff -merge
data/analysis/**/*.png  -filter -diff -merge
data/analysis/**/*.jpeg -filter -diff -merge
# SVG visual artifacts (designer-authored flow diagrams, UI mockups,
# vector reproductions of generated figures) ship as plain git so PR
# diffs surface real vector changes (LFS pointers render as text
# blobs) and raw.githubusercontent.com URLs serve the bytes directly
# (LFS pointers don't resolve over raw). Same rationale as the
# PDF/PNG/JPEG exemptions above. Files are typically <1.5 MB.
data/analysis/**/*.svg  -filter -diff -merge

# Build / run metadata JSONs ship as plain git so PR diffs surface
# changes to provenance + summary stats (LFS-pointer renders as a
# ~130-byte text blob, hiding what actually changed). Each file is
# <10 KB, total ~150 KB across ~35 files.
**/*_summary.json    -filter -diff -merge text
**/*.summary.json    -filter -diff -merge text
**/*_traceability.json -filter -diff -merge text
**/*_metadata.json   -filter -diff -merge text
**/metadata.json     -filter -diff -merge text

# Small reference TSVs that ship as plain git so a fresh `git clone`
# can run the genome-wide sweeps without an LFS-hydration step. The
# 3.4 MB upstream NCBI protein-coding catalog is the canonical input
# for the 19,464-gene sweep; the .triageable view drops the 140
# structural artifacts (readthrough fusions, pseudogenes, RNA-only,
# HGNC-no-UniProt) — see scripts/build_triageable_catalog.py. Both
# well below GitHub's 50 MB soft limit.
data/external/ncbi_gene_info/Homo_sapiens.protein_coding.with_hgnc.tsv -filter -diff -merge text
data/external/ncbi_gene_info/Homo_sapiens.protein_coding.with_hgnc.triageable.tsv -filter -diff -merge text

# Candidate-universe TSV is small (~1.4 MB) and the canonical data
# source for figure-reproduction gists linked from blog posts; ship
# as plain git so raw.githubusercontent.com can serve the file
# content directly (LFS pointers don't resolve over the raw URL).
data/processed/candidate_universe/candidate_universe.tsv -filter -diff -merge text
data/processed/candidate_universe/candidate_universe_v2.tsv -filter -diff -merge text
data/processed/candidate_universe/candidate_universe_v3.tsv -filter -diff -merge text
data/processed/candidate_universe/candidate_universe_v3_dropped.tsv -filter -diff -merge text

# Canonical SurfaceBench predictions TSV — refreshed from public D1
# by `scripts/export_mainbench_to_tsv.py` and pinned to a commit SHA
# at publication for stable citation. Figure scripts + published gists
# fetch it via raw.githubusercontent.com, so it stays non-LFS (LFS
# pointers don't resolve over the raw URL). The Worker also serves
# the same shape at `/v1/triage/export.tsv` for live consumers.
data/processed/triage_bench/mainbench_canonical_v2.tsv -filter -diff -merge text
data/processed/triage_bench/mainbench_replicates_v2.tsv -filter -diff -merge text

# Re-confirm sweep gene list (~94 KB plaintext) — input to
# `scripts/triage_runner.py --gene-list`; small enough to diff
# inline and there's no reason to LFS it.
data/processed/reconfirm_sonnet_no_zero_db_v1/gene_list.tsv -filter -diff -merge text

# 147-gene triage benchmark with ground-truth verdicts. Stays non-LFS
# because (a) `scripts/upload_benchmark_to_d1.py` reads it as the
# source-of-truth input to the D1 sync, and (b) figure scripts fetch
# it via raw.githubusercontent.com for ground-truth labels alongside
# the predictions TSV above.
data/eval/triage_benchmark_v1.tsv -filter -diff -merge text

# DB cutoff-tradeoff precomputed points dumped by
# scripts/triage_bench_db_barplot.py::_dump_db_cutoff_tradeoff_points.
# Tiny (~1.5 KB); raw-fetched by the figures-folder gist script.
data/processed/triage_bench/db_cutoff_tradeoff_points.tsv -filter -diff -merge text

# Optimized-cutoffs accession TSV dumped by
# scripts/triage_bench_db_barplot.py::_dump_db_optimized_cutoffs.
# ~50 KB; raw-fetched by the figures-folder by_class gist so it can
# reproduce the optimized-cutoffs view of the plot.
data/processed/triage_bench/db_optimized_cutoffs.tsv -filter -diff -merge text

# Optimized DB-cutoff accession sets dumped by
# scripts/triage_bench_db_barplot.py::_dump_optimized_db_accs.
# Tiny single-column TSVs (~30/10 KB); raw-fetched by the
# figures-folder gist for db_correctness_by_class.
data/processed/triage_bench/uniprot_tm_signal_accs.tsv -filter -diff -merge text
data/processed/triage_bench/cspa_hc_only_accs.tsv       -filter -diff -merge text
data/processed/catalog/whole_proteome_catalog.tsv -filter -diff -merge text

# Paywall/bot-block reachability figure input — per-paper buckets for the
# production-vs-OpenAlex comparison figure. ~310KB, raw-fetched by the
# figures-folder gist (cbc950dad1c3a6595fd5018cdb6b030d).
data/processed/paywall_bot_block/paywall_bot_block_compare.tsv -filter -diff -merge text

# Figure-input TSV for the db-vs-sonnet inclusion-bias analysis —
# exempt from LFS so the standalone gist mirror at
# data/analysis/figures/make_topology_coverage_by_source.py can fetch
# it via raw.githubusercontent.com. ~2.8 MB, well under the 5 MB
# practical cap.
data/analysis/db_vs_sonnet_inclusion/per_protein_features.tsv -filter -diff -merge text
data/analysis/db_vs_sonnet_inclusion/inclusion_enrichment.tsv  -filter -diff -merge text

# Positive-control augmented indicator TSVs (ADC / TCE / ViralZone) — three
# small files (~5-10 KB each) raw-fetched by the figures-folder gist for the
# positive-control DB-coverage bars. Each row carries hgnc_id + denormalized
# stable IDs + 5-DB flags + Sonnet (NCBI dual-pass) flag.
data/processed/positive_controls/positive_control_ADC.tsv -filter -diff -merge text
data/processed/positive_controls/positive_control_TCE.tsv -filter -diff -merge text
data/processed/positive_controls/positive_control_VZ.tsv  -filter -diff -merge text
# Long-form combined TSV (one row per category × gene) + per-(category, source)
# coverage summary. Both are small (~10 KB), grep-friendly, and the summary is
# exactly the table the figure renders — checked in so a reviewer can verify
# counts without re-running the build.
data/processed/positive_controls/positive_control_long.tsv -filter -diff -merge text
data/processed/positive_controls/positive_control_db_coverage_summary.tsv -filter -diff -merge text

# ViralZone source CSV — mirrored from deliverome-internal:data/raw/ so the
# public reproduction does not depend on private-repo auth. Plain text;
# small (~33 KB).
data/external/viralzone/viralzone_human_entry_receptors.csv -filter -diff -merge text

# ADCdb antigen list — parsed from the IDRBlab ADCdb search pages
# (333 unique antigens, 302 with inline HGNC symbol). ~14 KB; read by
# build_positive_control_lists.py to fold ADCdb into the ADC union.
data/external/adcdb/adcdb_antigens.tsv -filter -diff -merge text
# ADCdb per-antigen Function-text scrape — curator-written descriptions from
# the per-antigen detail pages. ~50 KB. Read by build_positive_control_lists.py
# to apply a Function-text noise filter that targets canonical secreted /
# intracellular function phrases (chemokine ligands, RNA polymerase
# components, SNAREs, etc.) — all ADCdb-internal text, no external DBs.
data/external/adcdb/adcdb_antigen_functions.tsv -filter -diff -merge text

# Per-figure consolidated TSVs for the two positive-control / deterministic-
# features figures, built by `scripts/build_figure_tsvs.py` — each is bundled
# into its slug's gist as the single source the mirror reads (single-TSV-per-
# gist invariant). Exempted from LFS so the bundled copy + the
# raw.githubusercontent.com fallback both resolve to plain text. Small (~5 KB
# mock placeholder / ~30 KB long-form), well under the 5 MB practical cap.
data/processed/figures/positive_control_db_coverage_bars.tsv  -filter -diff -merge text
data/processed/figures/surfaceome_deterministic_features_placeholder.tsv -filter -diff -merge text
