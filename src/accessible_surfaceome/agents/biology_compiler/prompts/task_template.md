# Task template — Biology Compiler (A2)

> Stub. Filled in when v1.0.0 ships.

## Gene
{{ gene_block }}

## Triage record
{{ triage_record }}

## Deterministic features (read-only)
{{ deterministic_features }}

## Your task
Produce a `BiologicalContextDraft` JSON block with your own evidence ledger
slice (evidence IDs prefixed `a2_evi_NN`). Populate
`accessibility_modulation` sub-enums per the validators in the system prompt.
